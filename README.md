# Compilation of usefull bash aliases
## Usage
Clone this repository to your `home` dir.  
Then symlink the `.bash_aliases` file inside so it's loaded by the `.bashrc`
```
ln -s ~/bash_aliases/.bash_aliases ~/.bash_aliases
```